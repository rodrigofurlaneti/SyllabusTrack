# Copie este arquivo para .env e ajuste os valores antes de rodar o docker-compose
# cp .env.example .env

# Senha do SQL Server (mínimo 8 chars, maiúscula + número + especial)
SA_PASSWORD=SyllabusTrack@2024!

# Chave JWT — mude para um valor aleatório seguro (mínimo 32 chars)
JWT_SECRET_KEY=CHANGE_THIS_TO_A_RANDOM_SECURE_KEY_WITH_32_CHARS_MINIMUM
